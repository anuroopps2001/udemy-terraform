A networking module that should:
1. Create an VPC with given cidr block
2. Allow user to provide the configuration for multiple subnets:
   2.1 The user should be able to mark the subnet as public or private
   2.2 The user should be able to provide the cidr blocks
   2.3 The user should be able to provide the aws AZs