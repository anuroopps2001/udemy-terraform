locals {
  math       = 2 * 2         # Math Operators:= +,-,*,/,%
  equality   = 2 != 2        # Equality Operators:= ==, !=
  comparison = 2 >= 4        # Comparison Operators:= >,>=,<,<=
  logical    = true || false # Logical Operators:= ||, &&, !
}